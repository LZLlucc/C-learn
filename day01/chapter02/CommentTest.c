
#include <stdio.h>    //#include :预处理的命令 此行代码表示引入标准io的头文件

/*

注释的讲解：

1、注释的作用：
① 对程序中的代码进行解释、说明
② 用于调试程序


2、注释的分类：
   单行注释 、 多行注释

3、说明
① 注释掉的代码、信息，不参与编译过程
② 多行注释不能嵌套使用



*/

/* 

如下的函数是main()，作为程序的入口
int : 指明函数的返回值的类型 （暂时不用了解）

 */
int main(){

    //标准的输出函数，将数据输出到控制台上
    printf("hello,world!!");
    //printf("hello,world!!")

    return 0;
}

